{
  "name": "slidev",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "slidev export slides-*.md --with-toc --format pptx && mkdir -p dist && mv slides-*.pptx dist && rm -rf slides-*-export",
    "dev": "slidev"
  },
  "dependencies": {
    "@slidev/cli": "^51.6.0",
    "@slidev/theme-default": "^0.25.0",
    "@slidev/theme-seriph": "^0.25.0",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "playwright-chromium": "^1.52.0"
  }
}